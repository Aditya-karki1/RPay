{
  "name": "district-root",
  "version": "1.0.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "npm install --include=dev --prefix district && npm run build --prefix district && npm install --prefix server",
    "start": "node server/index.js"
  }
}
